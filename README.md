# PETROCAF Pricing Engine Full V1

GitHub-ready Python pricing engine for EPC / construction tender pricing.

## Quick start
```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
python scripts/run_pricing.py --config config/settings.json
```

## Important
This is a strong codebase and package structure.
Sample pricing values still need replacement with PETROCAF-approved real references before live tender use.
